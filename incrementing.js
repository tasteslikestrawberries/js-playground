let count = 0;
//count++//postfix returns the value first and then increments
console.log(count) // the count value is now 1

console.log(++count) //increments and then returns the (now incremented) value